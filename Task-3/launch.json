{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flask",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "console": "integratedTerminal"
        }
    ],
    "compounds": [
        {
            "name": "Run Flask and Open Browser",
            "configurations": ["Flask"],
            "preLaunchTask": "",
            "stopAll": true
        }
    ]
}